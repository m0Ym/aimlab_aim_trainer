# 研修报告撰写指南

## 文档格式要求

### 页面设置
- 纸张大小：A4
- 页边距：上 2.5cm，下 2.5cm，左 2.5cm，右 2.5cm
- 页眉页脚：1.5cm

### 字体要求
- 正文：小四号（12pt），宋体
- 一级标题：三号（16pt），黑体，加粗
- 二级标题：四号（14pt），黑体，加粗
- 三级标题：小四号（12pt），黑体，加粗
- 图表标题：五号（10.5pt），宋体，居中
- 代码：小五号（9pt），Consolas 或 Courier New

### 行距段距
- 行距：1.5 倍行距
- 段前段后：0 行

---

## 报告结构

### 封面
```
面向对象程序设计课程研修报告

题目：Aim Lab 练枪模拟器

学院：[学院名称]
专业：[专业名称]
班级：[班级]

小组成员：
姓名 1（学号 1）- 负责模块：XXX
姓名 2（学号 2）- 负责模块：XXX
姓名 3（学号 3）- 负责模块：XXX

指导教师：[教师姓名]
完成日期：2026 年 6 月 19 日
```

### 摘要（200-300 字）
简要描述项目的背景、目标、主要功能和实现的技术亮点。

**示例：**
> 本项目开发了一款基于 Pygame 的 FPS 瞄准训练模拟器，旨在通过游戏化的方式提升玩家的瞄准精度和反应速度。系统采用面向对象的设计方法，实现了多种靶子类型、实时数据追踪、自动化分析报告生成等功能。项目完整运用了继承、多态、封装等面向对象核心概念，并结合 Pandas、Matplotlib、python-docx 等库实现了数据分析与可视化。测试结果表明，该系统能够有效记录和分析用户的训练数据，为技能提升提供科学依据。

### 关键词
面向对象、Pygame、数据分析、可视化、自动化报告

---

## 正文部分

### 1. 项目描述

#### 1.1 项目背景
- FPS 游戏在青少年中的普及
- 专业练枪软件（如 Aim Lab）的流行
- 课程要求与实际应用相结合

#### 1.2 项目目标
- 实现一个可玩的练枪模拟器
- 运用面向对象的设计方法
- 实现数据分析和自动化报告
- 满足课程评分标准

#### 1.3 功能特性
- 自定义准星系统
- 多种靶子类型（普通靶、闪现靶、追踪靶）
- 实时计分和生命值
- 数据追踪和分析
- Word 报告自动生成

### 2. 设计思路

#### 2.1 系统架构
（插入系统架构图）

```
┌─────────────────────────────────────────┐
│           用户界面层                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │ 主菜单  │  │ 游戏界面│  │ 结算界面│ │
│  └─────────┘  └─────────┘  └─────────┘ │
└─────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│           游戏逻辑层                      │
│  ┌──────────────┐  ┌──────────────┐     │
│  │ GameManager  │  │  Target 系统 │     │
│  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│           数据处理层                      │
│  ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │DataTracker│ │Analyzer │ │Visualizer│ │
│  └──────────┘ └──────────┘ └─────────┘ │
└─────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│           报告生成层                      │
│  ┌──────────────────────────────────┐   │
│  │      ReportGenerator (Word)      │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

#### 2.2 类图设计
（插入 UML 类图）

**核心类说明：**

1. **GameManager**（游戏管理器）
   - 职责：控制游戏状态、处理输入、管理游戏循环
   - 关键方法：`run()`, `handle_events()`, `update()`, `draw()`

2. **Target**（目标基类）
   - 职责：定义目标的基本属性和行为
   - 关键方法：`draw()`, `check_click()`, `update()`

3. **FlickTarget**（闪现靶）
   - 继承：Target
   - 特性：0.5 秒后自动消失
   - 重写方法：`update()`

4. **TrackingTarget**（追踪靶）
   - 继承：Target
   - 特性：按正弦波移动
   - 重写方法：`update()`

5. **DataTracker**（数据追踪器）
   - 职责：记录每次点击的详细信息
   - 关键方法：`record_click()`, `get_clicks()`

6. **DataAnalyzer**（数据分析器）
   - 职责：计算 TTK、命中率等指标
   - 关键方法：`process_data()`, `calculate_avg_ttk()`

7. **DataVisualizer**（数据可视化器）
   - 职责：生成图表
   - 关键方法：`create_heatmap()`, `create_reaction_time_plot()`

8. **ReportGenerator**（报告生成器）
   - 职责：生成 Word 报告
   - 关键方法：`create_report()`

#### 2.3 设计模式应用

1. **状态模式**
   - GameState 枚举管理游戏状态
   - 不同状态下行为自动切换

2. **工厂模式**
   - spawn_target() 方法根据概率生成不同类型的靶子

3. **观察者模式**
   - DataTracker 记录游戏事件
   - Analyzer 和 Visualizer 处理数据

### 3. 代码实现

#### 3.1 核心代码展示

**代码片段 1：Target 基类（组员 C）**
```python
class Target:
    """目标基类，所有靶子类型的父类"""
    
    def __init__(self, x, y, radius, color):
        self.x = x
        self.y = y
        self.radius = radius
        self.color = color
        self.is_alive = True
    
    def draw(self, screen):
        """在屏幕上绘制目标"""
        if self.is_alive:
            pygame.draw.circle(screen, self.color, 
                             (int(self.x), int(self.y)), self.radius)
    
    def check_click(self, mouse_x, mouse_y):
        """检查鼠标点击是否击中目标（勾股定理）"""
        distance = math.sqrt((mouse_x - self.x)**2 + **(mouse_y - self.y)2)
        return distance <= self.radius
```

**代码片段 2：FlickTarget 派生类（组员 B）**
```python
class FlickTarget(Target):
    """闪现靶类 - 0.5 秒后自动消失"""
    
    def __init__(self, x, y, radius, color):
        super().__init__(x, y, radius, color)
        self.lifetime = 0.5
        self.score_value = 150
    
    def update(self, delta_time):
        """重写 update 方法，实现自动消失"""
        elapsed = (pygame.time.get_ticks() - self.spawn_time) / 1000.0
        if elapsed >= self.lifetime:
            self.destroy()
```

**代码片段 3：DataAnalyzer 数据分析（组员 A）**
```python
def calculate_avg_ttk(self):
    """计算平均击杀时间"""
    hits_df = self.df[self.df['hit'] == True]
    hit_times = hits_df['timestamp'].tolist()
    
    if len(hit_times) < 2:
        return 0.0
    
    ttk_values = []
    for i in range(1, len(hit_times)):
        ttk = hit_times[i] - hit_times[i-1]
        ttk_values.append(ttk)
    
    return np.mean(ttk_values) * 1000  # 转换为毫秒
```

**代码片段 4：ReportGenerator 报告生成（组员 C）**
```python
def _add_metrics_table(self):
    """添加指标表格"""
    table = self.document.add_table(rows=7, cols=2)
    table.style = 'Table Grid'
    
    headers = [
        ('总射击次数', str(metrics['total_clicks'])),
        ('命中次数', str(metrics['hits'])),
        ('命中率', f'{metrics["accuracy"]:.1f}%'),
        # ... 更多指标
    ]
```

#### 3.2 代码统计

| 文件 | 行数 | 作者 | 主要功能 |
|------|------|------|----------|
| game_manager.py | 280+ | 组员 A | 游戏主循环 |
| target.py | 60+ | 组员 C | 目标基类 |
| target_types.py | 120+ | 组员 B | 派生类 |
| data_analyzer.py | 130+ | 组员 A | 数据分析 |
| data_visualizer.py | 150+ | 组员 B | 可视化 |
| report_generator.py | 200+ | 组员 C | 报告生成 |
| **总计** | **1100+** | - | - |

**注：每人代码行数均超过 150 行，满足课程要求。**

### 4. 调试过程

#### 4.1 典型 Bug 及解决方案

**Bug 1：靶子生成在屏幕外**
- 现象：部分靶子看不到
- 原因：生成坐标时未考虑靶子半径
- 解决：
```python
# 错误代码
x = random.randint(0, SCREEN_WIDTH)

# 正确代码
x = random.randint(radius, SCREEN_WIDTH - radius)
```

**Bug 2：数据越界错误**
- 现象：计算 TTK 时报错
- 原因：空数据未做处理
- 解决：
```python
if len(hit_times) < 2:
    return 0.0
```

**Bug 3：图表中文乱码**
- 现象：图表标题显示乱码
- 原因：Matplotlib 默认不支持中文
- 解决：配置中文字体或使用英文

#### 4.2 调试截图
（插入调试过程中的截图，3-5 张）

### 5. AI 使用说明

#### 5.1 使用场景
1. 架构设计咨询
2. 代码片段生成
3. 算法实现建议
4. Bug 调试辅助
5. 文档编写

#### 5.2 Prompt 示例
```
"我需要实现三种靶子类型，使用继承和多态：
- Target 基类：基本绘制和点击检测
- FlickTarget：0.5 秒后消失
- TrackingTarget：按正弦波移动
请提供 Python 代码实现。"
```

#### 5.3 AI 贡献统计
- AI 生成代码比例：约 40%
- 手动修改比例：约 60%
- 主要帮助领域：类结构设计、算法实现、图表美化

#### 5.4 使用心得
- AI 适合生成样板代码
- 边界条件需要人工补充
- 业务逻辑需要人工设计
- 代码质量需要人工审查

### 6. 测试结果

#### 6.1 功能测试
- ✅ 游戏可以正常运行
- ✅ 准星平滑跟随鼠标
- ✅ 三种靶子行为正确
- ✅ 计分系统准确
- ✅ 数据记录完整
- ✅ 报告自动生成

#### 6.2 性能测试
- 帧率：稳定 60 FPS
- 内存占用：< 200MB
- 报告生成时间：< 2 秒

#### 6.3 示例报告
（插入生成的 Word 报告截图）

### 7. 结论与展望

#### 7.1 项目总结
- 成功实现了所有预期功能
- 代码结构清晰，符合面向对象原则
- 数据分析和报告生成是亮点
- 团队成员代码量均衡

#### 7.2 收获与体会
- 深入理解了面向对象设计
- 掌握了 Pygame 游戏开发
- 学会了数据可视化技术
- 提升了团队协作能力

#### 7.3 改进方向
- 添加更多靶子类型（爆头靶、移动靶）
- 实现多人排行榜
- 增加训练模式（反应训练、跟枪训练）
- 优化 UI 界面

---

## 参考文献

[1] pygame 官方文档。https://www.pygame.org/docs/
[2] pandas 官方文档。https://pandas.pydata.org/docs/
[3] Matplotlib 官方文档。https://matplotlib.org/stable/contents.html
[4] python-docx 官方文档。https://python-docx.readthedocs.io/

---

## 附录

### 附录 A：小组成员分工
- 组员 A：GameManager、DataAnalyzer（核心逻辑和数据分析）
- 组员 B：Target 派生类、DataVisualizer（多态实现和可视化）
- 组员 C：Target 基类、DataTracker、ReportGenerator（数据管理和报告）

### 附录 B：运行说明
1. 安装 Python 3.8+
2. 运行 `pip install -r requirements.txt`
3. 运行 `python main.py` 或双击 `run.bat`

### 附录 C：提交材料清单
- [x] 研修报告（PDF）
- [x] 源代码文件夹
- [x] 程序演示视频
- [x] 工作照片（3 张）
- [x] AI 使用说明文档
- [x] 示例报告

---

**（报告结束）**
